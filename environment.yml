name: qiime2-amplicon-2024.5
channels:
  - https://packages.qiime2.org/qiime2/2024.5/amplicon/released
  - conda-forge
  - bioconda
  - defaults
dependencies:
  # QIIME2 Amplicon Distribution 2024.5 (versión estable)
  - qiime2-amplicon

  # Herramientas adicionales para análisis de microbioma
  - picrust2
  - sra-tools

  # Python y paquetes base
  - python=3.9
  - pip

  # Paquetes de Python via pip
  - pip:
    - dokdo==1.16.0